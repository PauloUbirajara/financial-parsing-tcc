version: "3"

services:
  sonarqube-db:
    image: postgres:latest
    networks:
      - sonarnet
    env_file:
      - .devcontainer/sonarqube-db.env
    volumes:
      - sonarqube_db_vol:/var/lib/postgresql
      - sonarqube_db_vol_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U sonar" ]
      interval: 30s
      timeout: 30s
      retries: 3

  sonarqube:
    image: sonarqube:latest
    ports:
      - "9000:9000"
    networks:
      - sonarnet
    env_file:
      - .devcontainer/sonarqube.env
    depends_on:
      sonarqube-db:
        condition: service_healthy
    volumes:
      - sonarqube_conf:/opt/sonarqube/conf
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
      - sonarqube_bundled-plugins:/opt/sonarqube/lib/bundled-plugins

  app-db:
    image: postgres:latest
    networks:
      - app-network
    env_file:
      - .devcontainer/app-db.env
    ports:
      - "5432:5432"
    volumes:
      - app_db_vol:/var/lib/postgresql
      - app_db_vol_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U sonar" ]
      interval: 30s
      timeout: 30s
      retries: 3

  app:
    networks:
      - app-network
    build:
      context: .
      dockerfile: .devcontainer/app.Dockerfile
    env_file:
      - .devcontainer/app.env
    volumes:
      - .:/app
    ports:
      - "3000:3000"


networks:
  sonarnet:
    driver: bridge
  app-network:
    driver: bridge

volumes:
  sonarqube_conf:
  sonarqube_data:
  sonarqube_extensions:
  sonarqube_bundled-plugins:
  sonarqube_db_vol:
  sonarqube_db_vol_data:
  app_db_vol:
  app_db_vol_data:
